package com.amine.spring_erp_platform.stock.entity;

public enum MovementType {
    IN,  // Entrée de stock (ex: livraison fournisseur)
    OUT  // Sortie de stock (ex: vente client ou perte)
}
