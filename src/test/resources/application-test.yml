spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false # On désactive Flyway pour ne pas avoir de conflits de scripts en test

app:
  jwt:
    secret: ***REMOVED***
    expiration: 86400000